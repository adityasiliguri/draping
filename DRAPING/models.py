from flask_sqlalchemy import SQLAlchemy
from datetime import datetime

db = SQLAlchemy()

# Models are defined in app.py for simplicity.
# If you want to split, move models here and import in app.py.
